# TikTok Auto Upload

Автоматическое загрузка и публикация видео на TikTok с использованием разных аккаунтов (через cookies).

## 🆕 Новые возможности

- **🆓 Бесплатные прокси**: Интеграция с free-proxy для автоматического получения рабочих прокси
- **🔄 Умная ротация**: Автоматическая смена IP для безопасности аккаунтов  
- **📊 Кэширование**: Повторное использование проверенных прокси
- **🌍 Фильтрация по странам**: Выбор прокси из предпочитаемых регионов
- **⚡ Автотестирование**: Проверка работоспособности прокси перед использованием

## Описание

Программа автоматически обрабатывает все файлы cookies из папки `cookies`, последовательно:
1. **Получает рабочий прокси** (бесплатный или платный)
2. Обновляет IP-адрес прокси перед работой с каждым аккаунтом
3. Загружает cookies из файла
4. Проверяет валидность cookies (успешность авторизации)
5. Загружает видео из папки `videos` на TikTok Studio
6. Автоматически публикует видео, нажимая на кнопку "Опубликовать"
7. Помечает обработанные cookies как `valid_` или `invalid_`

## Требования

- Python 3.8+
- Playwright
- Firefox браузер
- Библиотеки: aiohttp, asyncio, free-proxy, lxml

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
playwright install firefox
```

2. Настройте прокси в файле `.env`:

### Для бесплатных прокси (рекомендуется для тестирования):
```env
PROXY_MODE=free
FREE_PROXY_COUNTRIES=US,GB,DE,CA,AU
FREE_PROXY_ANONYM=true
USE_PROXY_ROTATION=true
```

### Для платных прокси (рекомендуется для продакшена):
```env
PROXY_MODE=paid
PROXY_SERVER=your-proxy-server.com
PROXY_PORT=8080
PROXY_USERNAME=your-username
PROXY_PASSWORD=your-password
```

3. Подготовьте файлы:
   - Поместите видео для загрузки в папку `videos/`
   - Поместите файлы с cookies в папку `cookies/`

## Использование

### Основной запуск:
```bash
python main.py
```

### Тестирование прокси:
```bash
# Тест интеграции бесплатных прокси
python test_free_proxy_integration.py

# Тест TikTokManager с прокси
python test_tiktok_with_free_proxy.py

# Пример использования free-proxy
python free-proxy/example_usage.py
```

## Структура проекта

- `main.py` - точка входа в программу
- `tiktok_manager.py` - основной класс для управления процессом загрузки и публикации видео
- `tiktok_cookies_loader.py` - класс для работы с cookies файлами
- `cookies/` - папка с файлами cookies
- `videos/` - папка с видео для загрузки

## Функциональность

### Управление cookies
- Поддержка различных форматов cookies (JSON, Netscape, текстовый)
- Автоматическая обработка всех файлов cookies в папке
- Пометка файлов как valid_ или invalid_ в зависимости от результата

### Управление прокси
- Автоматическое обновление IP-адреса прокси перед работой с каждым аккаунтом
- Использование API ASocks для смены IP

### Загрузка и публикация видео
- Автоматическая загрузка видео на TikTok Studio
- Заполнение необходимых полей (описание, хэштеги)
- Автоматическая публикация видео через нажатие кнопки "Опубликовать"
- Проверка успешности публикации

## Примечания

- Обработанные файлы cookies получают префиксы `valid_` или `invalid_`
- При повторном запуске, уже обработанные файлы пропускаются
- Для загрузки используется первое видео из папки `videos/`
- Перед каждым аккаунтом обновляется IP-адрес прокси для безопасности 