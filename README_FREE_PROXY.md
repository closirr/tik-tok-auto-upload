# TikTok Auto Upload с бесплатными прокси

Автоматизированный инструмент для загрузки видео в TikTok с поддержкой бесплатных прокси через библиотеку free-proxy.

## 🆓 Новые возможности

- **Бесплатные прокси**: Автоматическое получение рабочих прокси из открытых источников
- **Умное кэширование**: Повторное использование проверенных прокси
- **Ротация прокси**: Автоматическая смена IP для безопасности аккаунтов
- **Фильтрация по странам**: Выбор прокси из предпочитаемых стран
- **Автоматическое тестирование**: Проверка работоспособности прокси перед использованием

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка основных зависимостей
pip install -r requirements.txt

# Установка браузера для Playwright
playwright install firefox
```

### 2. Настройка бесплатных прокси

Отредактируйте файл `.env`:

```env
# Режим прокси: 'free' для бесплатных, 'paid' для платных
PROXY_MODE=free

# Настройки бесплатных прокси
FREE_PROXY_COUNTRIES=US,GB,DE,CA,AU  # Предпочитаемые страны
FREE_PROXY_HTTPS=false               # Требовать HTTPS прокси
FREE_PROXY_ANONYM=true               # Требовать анонимные прокси
FREE_PROXY_TIMEOUT=5.0               # Таймаут проверки (секунды)
FREE_PROXY_POOL_SIZE=5               # Размер пула для ротации
FREE_PROXY_CACHE_TIME=300            # Время кэширования (секунды)

# Ротация прокси
USE_PROXY_ROTATION=true
```

### 3. Подготовка файлов

```bash
# Добавьте куки в папку cookies/
# Формат: extracted_<id>_<browser>_<profile>.txt

# Добавьте видео в папку videos/
# Поддерживаемые форматы: .mp4, .mov, .avi
```

### 4. Запуск

```bash
# Основной запуск
python main.py

# Тестирование интеграции прокси
python test_free_proxy_integration.py

# Тестирование TikTokManager
python test_tiktok_with_free_proxy.py
```

## ⚙️ Конфигурация прокси

### Режимы работы

1. **Бесплатные прокси** (`PROXY_MODE=free`)
   - Автоматическое получение из открытых источников
   - Проверка работоспособности
   - Кэширование и ротация

2. **Платные прокси** (`PROXY_MODE=paid`)
   - Использование настроенных прокси-серверов
   - API для обновления IP
   - Стабильное соединение

### Настройки бесплатных прокси

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `FREE_PROXY_COUNTRIES` | Коды стран через запятую | `US,GB,DE,CA,AU` |
| `FREE_PROXY_HTTPS` | Требовать HTTPS прокси | `false` |
| `FREE_PROXY_ANONYM` | Требовать анонимность | `true` |
| `FREE_PROXY_TIMEOUT` | Таймаут проверки (сек) | `5.0` |
| `FREE_PROXY_POOL_SIZE` | Размер пула ротации | `5` |
| `FREE_PROXY_CACHE_TIME` | Время кэша (сек) | `300` |

### Коды стран

Поддерживаемые коды стран для `FREE_PROXY_COUNTRIES`:

- `US` - США
- `GB` - Великобритания  
- `DE` - Германия
- `CA` - Канада
- `AU` - Австралия
- `FR` - Франция
- `NL` - Нидерланды
- `JP` - Япония
- `KR` - Южная Корея
- `SG` - Сингапур

## 🔧 Тестирование

### Проверка конфигурации

```bash
python test_free_proxy_integration.py
```

Этот скрипт проверит:
- ✅ Настройки конфигурации
- ✅ Получение рабочих прокси
- ✅ Создание пула прокси
- ✅ Ротацию прокси
- ✅ Кэширование

### Проверка TikTokManager

```bash
python test_tiktok_with_free_proxy.py
```

Этот скрипт проверит:
- ✅ Инициализацию менеджера
- ✅ Обновление прокси
- ✅ Конфигурацию для Playwright

## 📊 Мониторинг

### Логи прокси

Система выводит подробные логи:

```
🔍 Поиск бесплатного прокси...
✅ Найден прокси: http://1.2.3.4:8080
✅ Прокси работает! IP: 1.2.3.4
🔄 Ротация прокси: http://5.6.7.8:3128
📋 Использую кэшированный прокси: http://1.2.3.4:8080
```

### Скриншоты

Скриншоты сохраняются в `screenshots/<cookie_name>_<timestamp>/`:
- `proxy_check_*.png` - Проверка прокси
- `tiktok_*.png` - Процесс загрузки
- `proxy_report.txt` - Отчет о прокси

## 🔄 Переключение между режимами

### На бесплатные прокси

```env
PROXY_MODE=free
```

### На платные прокси

```env
PROXY_MODE=paid
PROXY_SERVER=your-proxy-server.com
PROXY_PORT=8080
PROXY_USERNAME=your-username
PROXY_PASSWORD=your-password
```

## ⚠️ Важные замечания

### Бесплатные прокси

- **Стабильность**: Могут быть нестабильными
- **Скорость**: Обычно медленнее платных
- **Доступность**: Могут периодически недоступны
- **Безопасность**: Используйте только для тестирования

### Рекомендации

1. **Для тестирования**: Используйте бесплатные прокси
2. **Для продакшена**: Используйте платные прокси
3. **Мониторинг**: Следите за логами работы
4. **Ротация**: Включайте ротацию для безопасности

## 🛠️ Устранение неполадок

### Прокси не работают

```bash
# Проверьте доступность источников прокси
python -c "from fp.fp import FreeProxy; print(FreeProxy().get())"

# Очистите кэш
python -c "from free_proxy_integration import get_proxy_manager; get_proxy_manager().clear_cache()"
```

### Медленная работа

```env
# Уменьшите таймаут
FREE_PROXY_TIMEOUT=3.0

# Увеличьте размер пула
FREE_PROXY_POOL_SIZE=10
```

### Ошибки подключения

```env
# Попробуйте другие страны
FREE_PROXY_COUNTRIES=US,CA,AU

# Отключите требование HTTPS
FREE_PROXY_HTTPS=false
```

## 📝 Логи и отладка

### Включение подробных логов

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Проверка IP

```bash
# Через встроенную проверку
python check_proxy.py

# Через free-proxy тест
python free-proxy/example_usage.py
```

## 🎯 Заключение

Интеграция с free-proxy позволяет:

- ✅ Использовать бесплатные прокси для тестирования
- ✅ Автоматически находить рабочие прокси
- ✅ Экономить на прокси-сервисах
- ✅ Легко переключаться между режимами
- ✅ Масштабировать работу с аккаунтами

Для продакшена рекомендуется использовать платные прокси (`PROXY_MODE=paid`).