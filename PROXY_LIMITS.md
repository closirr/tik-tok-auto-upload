# Лимиты поиска прокси

## Проблема

Система могла бесконечно пытаться получить рабочий прокси, если все источники были недоступны, что приводило к зависанию обработки.

## Решение

Добавлены глобальные лимиты на количество неудачных попыток получения прокси.

## Настройки лимитов

### Глобальные лимиты (FreeProxyManager)

```python
self.max_total_failures = 300      # Максимум неудачных попыток всего
self.max_consecutive_failures = 300 # Максимум подряд идущих неудач
```

### Локальные лимиты (TikTokManager)

```python
max_proxy_attempts = 3  # Попытки на один аккаунт
max_proxy_retries = 2   # Попытки смены прокси в браузере
```

## Логика работы

### 1. Проверка лимитов перед поиском

```python
if self.total_failed_attempts >= self.max_total_failures:
    print("🛑 Достигнут лимит неудачных попыток (300)")
    return None

if self.consecutive_failures >= self.max_consecutive_failures:
    print("🛑 Слишком много подряд идущих неудач (300)")
    return None
```

### 2. Счетчики неудач

- **total_failed_attempts**: Общее количество неудач с момента запуска
- **consecutive_failures**: Подряд идущие неудачи (сбрасывается при успехе)

### 3. Автоматическая остановка

Когда достигнут любой из лимитов:
- Поиск прокси блокируется
- Обработка аккаунтов останавливается
- Выводится информация о статистике

## Использование

### Проверка статуса

```bash
# Запуск main.py автоматически показывает статус
python main.py
```

Вывод:
```
📊 Статус бесплатных прокси:
   Всего неудач: 45/300
   Подряд неудач: 12/300
   Осталось попыток: 255
```

### Сброс счетчиков

```bash
# Полный сброс всех счетчиков
python reset_proxy_counters.py

# Только тест без сброса
python reset_proxy_counters.py --test
```

### Мониторинг в коде

```python
from free_proxy_integration import get_proxy_manager

manager = get_proxy_manager()
stats = manager.get_failure_stats()

print(f"Всего неудач: {stats['total_failed']}/{stats['max_total']}")
print(f"Заблокирован: {stats['is_blocked']}")
print(f"Осталось попыток: {stats['remaining_attempts']}")
```

## Сценарии использования

### Нормальная работа
- Система находит рабочие прокси
- Счетчик consecutive_failures сбрасывается при успехе
- Обработка продолжается

### Временные проблемы с прокси
- Накапливаются неудачи, но не достигают лимита
- При появлении рабочего прокси счетчик consecutive сбрасывается
- Обработка восстанавливается

### Полная недоступность прокси
- Достигается лимит в 300 неудач
- Система останавливает поиск прокси
- Требуется ручной сброс счетчиков

## Настройка лимитов

Для изменения лимитов отредактируйте `free_proxy_integration.py`:

```python
# Для более строгих лимитов
self.max_total_failures = 100
self.max_consecutive_failures = 50

# Для более мягких лимитов  
self.max_total_failures = 500
self.max_consecutive_failures = 500
```

## Рекомендации

1. **Мониторинг**: Регулярно проверяйте статистику неудач
2. **Сброс**: Сбрасывайте счетчики при смене источников прокси
3. **Лимиты**: Настройте лимиты под ваши потребности
4. **Логи**: Следите за сообщениями о блокировке поиска

Теперь система не будет зависать при недоступности прокси и предоставит четкую информацию о проблемах!